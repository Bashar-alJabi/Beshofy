import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	// output: "export",
	// basePath: "/Beshofy",
	// assetPrefix: "/Beshofy/",
	// images: {
	// 	unoptimized: true,
	// },
};

export default nextConfig;
